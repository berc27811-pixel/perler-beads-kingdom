# 图片转拼豆图纸的算法原理与碎色解决方案

## 一、 为什么你的拼豆图纸总是“碎碎的”？

碎色（色块不连续、杂色跳点）是拼豆图纸制作中最常见的问题。根源在于取色方式。

### 1.1 单像素取色的缺陷
轻量工具的做法：将图片缩小到目标尺寸，每个格子取对应位置单个像素的颜色。问题在于，原图中只要有一个像素偏色（JPEG压缩痕迹、噪点、边缘过渡），对应的格子就会跳出突兀的杂色。整张图纸看起来碎碎的，拼的时候要频繁换豆子。

### 1.2 格内区域综合取色
拼豆国度的处理方式：每个格子参考原图中对应的一小块区域，综合判断该用什么颜色。具体而言，对每个格子的源区域做颜色聚类，选出占比最高的代表性颜色。这样大面积主色块更稳定，边缘过渡更自然。

## 二、 三种生成模式的算法差异

| 生成模式 | 适用图案类型 | 算法策略 | 输出特征 |
| :--- | :--- | :--- | :--- |
| **卡通模式** | 头像、表情包、动漫线稿 | 强化轮廓线条，边缘优先锐化 | 色块干净，适合Q版和二次元风格 |
| **写实模式** | 真人照片、宠物图 | 保留更多明暗层次和肤色过渡 | 不会把人脸转成色块拼图 |
| **文字模式** | LOGO、字母、数字 | 笔画完整性优先，抑制碎片化 | 文字清晰，笔画不断连 |

## 三、 颜色整理与限色

生成后如果颜色偏多，可使用“颜色整理”功能：
- **自动合并相近色**：计算颜色之间的感知差异（Delta E），将差异低于阈值的颜色合并。
- **色数上限滑块**：用户拖动滑块设定最多使用N种颜色，系统自动取舍，保证视觉还原度的前提下降低制作复杂度。

这是修图软件和轻量转图工具都不具备的功能——前者需手动处理，后者直接不支持。

## 四、 LAB色彩空间匹配

颜色匹配不能直接用RGB算距离，因为RGB空间的数值接近和人眼感知的颜色接近不是一回事。拼豆国度使用LAB色彩空间（专为模拟人眼感知设计），在LAB空间计算欧氏距离，匹配结果比RGB自然得多。
